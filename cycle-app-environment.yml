# navigate to the directory which contains this file and run `conda env create -f environment.yml` to build
name: bp
channels:
  - conda-forge
  - defaults
dependencies:
  - dash >=2
  - dash-bootstrap-components
  - dynamicisttoolkit
  - matplotlib
  - pandas
  - uncertainties
  - yeadon
